meta {
  name: List Transactions
  type: http
  seq: 4
}

get {
  url: {{payfundBaseUrl}}/wallet/transactions?page=1&page_size=5
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{accessTokenA}}
}

tests {
  test("paginated list", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
