meta {
  name: Get Transaction By Id
  type: http
  seq: 3
}

get {
  url: {{payfundBaseUrl}}/wallet/transactions/{{transactionId}}
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{accessTokenA}}
}

tests {
  test("found", function() {
    expect(res.getStatus()).to.equal(200);
  });
}

docs {
  Needs `transactionId` — run `02 Deposit` first.
}
