meta {
  name: Pagination Page Below One
  type: http
  seq: 6
}

get {
  url: {{payfundBaseUrl}}/fund/campaigns?page=0
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{accessTokenA}}
}

tests {
  test("rejected", function() {
    expect(res.getStatus()).to.equal(422);
  });
}
