meta {
  name: Create Campaign Empty Title
  type: http
  seq: 4
}

post {
  url: {{payfundBaseUrl}}/fund/campaigns
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{accessTokenA}}
}

body:json {
  {
    "title": "",
    "goal_amount": 1000
  }
}

tests {
  test("rejected", function() {
    expect(res.getStatus()).to.equal(422);
  });
}
