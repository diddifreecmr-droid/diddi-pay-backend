meta {
  name: Invalid List Limit
  type: http
  seq: 12
}

get {
  url: {{payfundBaseUrl}}/payment-intents?limit=101
  body: none
  auth: none
}

headers {
  X-Client-ID: {{paymentClientId}}
  X-Service-Key: {{paymentServiceKey}}
}

tests {
  test("list limit is bounded", function() {
    expect(res.getStatus()).to.equal(422);
  });
}
