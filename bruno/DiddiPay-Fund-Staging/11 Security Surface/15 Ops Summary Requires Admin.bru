meta {
  name: Ops Summary Requires Admin
  type: http
  seq: 15
}

get {
  url: {{payfundBaseUrl}}/wallet/ops/paystack/summary
  body: none
  auth: none
}

tests {
  test("reconciliation summary requires authentication", function() {
    expect(res.getStatus()).to.equal(401);
  });
}
