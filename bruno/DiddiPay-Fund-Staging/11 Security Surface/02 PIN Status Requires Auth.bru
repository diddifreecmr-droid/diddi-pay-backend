meta {
  name: PIN Status Requires Auth
  type: http
  seq: 2
}

get {
  url: {{payfundBaseUrl}}/wallet/pin
  body: none
  auth: none
}

tests {
  test("PIN status is protected", function() {
    expect(res.getStatus()).to.equal(401);
  });
}
