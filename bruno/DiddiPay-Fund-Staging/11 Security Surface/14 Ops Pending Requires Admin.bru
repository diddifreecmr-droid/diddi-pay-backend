meta {
  name: Ops Pending Requires Admin
  type: http
  seq: 14
}

get {
  url: {{payfundBaseUrl}}/wallet/ops/paystack/pending
  body: none
  auth: none
}

tests {
  test("pending deposits require authentication", function() {
    expect(res.getStatus()).to.equal(401);
  });
}
