meta {
  name: Get My Profile (User A)
  type: http
  seq: 8
}

get {
  url: {{identityBaseUrl}}/users/me
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{accessTokenA}}
}

tests {
  test("profile fetched", function() {
    expect(res.getStatus()).to.equal(200);
  });
}

docs {
  Proves the whole login round-trip works end to end — the same token payfund itself verifies.
}
