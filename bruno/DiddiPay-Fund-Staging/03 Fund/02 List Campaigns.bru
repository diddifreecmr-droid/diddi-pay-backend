meta {
  name: List Campaigns
  type: http
  seq: 2
}

get {
  url: {{payfundBaseUrl}}/fund/campaigns?page=1&page_size=10
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{accessTokenA}}
}

tests {
  test("paginated list", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
